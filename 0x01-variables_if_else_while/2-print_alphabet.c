#include <stdio.h>

/**
 * main - Entry point
 * main - prints the alphabet in lowercase, followed by a new line
 * Return: 0 Is a success.
 */

int main(void)
{
	char s
	s = 'a';
	while
		(s <= 'z') {
		putchar(s);
		s++;
		}
	putchar('\n');
	return (0);
}

